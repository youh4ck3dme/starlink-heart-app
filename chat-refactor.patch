diff --git a/src/components/StarlinkHeartApp.tsx b/src/components/StarlinkHeartApp.tsx
index f7eb313..6bb580f 100755
--- a/src/components/StarlinkHeartApp.tsx
+++ b/src/components/StarlinkHeartApp.tsx
@@ -102,32 +102,7 @@ const FormatText = ({ text }: { text: string }) => {
     );
 };
 
-// StarryAvatarDisplay is now integrated inside ChatView but used here for Dashboards
-const StarryAvatarDisplay = ({ 
-    avatar, 
-    isThinking = false, 
-    isExcited = false, 
-    size = "text-3xl" 
-}: { 
-    avatar: string; 
-    isThinking?: boolean; 
-    isExcited?: boolean; 
-    size?: string; 
-}) => {
-    return (
-        <div className={`relative flex items-center justify-center ${size} transition-all duration-300`}>
-            <div className={`relative z-10 transition-transform duration-500 ${isExcited ? 'scale-125 rotate-[360deg]' : 'scale-100'} ${isThinking ? 'animate-bounce' : ''}`}>
-                {avatar}
-            </div>
-            {isThinking && (
-                 <span className="absolute -top-1 -right-1 flex h-3 w-3">
-                   <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-sky-400 opacity-75"></span>
-                   <span className="relative inline-flex rounded-full h-3 w-3 bg-sky-500"></span>
-                 </span>
-            )}
-        </div>
-    );
-};
+import StarryAvatarDisplay from './common/StarryAvatarDisplay';
 
 // --- Game Style Components ---
 
diff --git a/src/components/chat/ChatView.tsx b/src/components/chat/ChatView.tsx
index 5f0a157..e91c62a 100644
--- a/src/components/chat/ChatView.tsx
+++ b/src/components/chat/ChatView.tsx
@@ -4,29 +4,7 @@ import ChatMessage from './ChatMessage';
 import ChatInput from './ChatInput';
 
 // StarryAvatarDisplay helper component
-const StarryAvatarDisplay = ({ 
-    avatar, 
-    isThinking = false, 
-    size = "text-3xl" 
-}: { 
-    avatar: string; 
-    isThinking?: boolean; 
-    size?: string; 
-}) => {
-    return (
-        <div className={`relative flex items-center justify-center ${size} transition-all duration-300`}>
-            <div className={`relative z-10 transition-transform duration-500 ${isThinking ? 'animate-bounce' : ''}`}>
-                {avatar}
-            </div>
-            {isThinking && (
-                <span className="absolute -top-1 -right-1 flex h-3 w-3">
-                    <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-sky-400 opacity-75"></span>
-                    <span className="relative inline-flex rounded-full h-3 w-3 bg-sky-500"></span>
-                </span>
-            )}
-        </div>
-    );
-};
+import StarryAvatarDisplay from '../common/StarryAvatarDisplay';
 
 interface ChatViewProps {
     hearts: Heart[];
